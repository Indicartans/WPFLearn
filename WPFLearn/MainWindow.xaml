<Window x:Class="WPFLearn.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFLearn"
        xmlns:userControls="clr-namespace:WPFLearn.View.UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Background="LightBlue">
            <TextBlock Text="Name"/>
            <Expander>
                <TextBlock Text="Phone"/>
            </Expander>

            <Button x:Name="btnDetails" Content="Details" Width="120" Click="btnDetails_Click"/>
        </StackPanel>

        <Expander x:Name="expanderDetails" Grid.Column="1" Background="LightCoral" BorderThickness="0" ExpandDirection="Left">
            <Rectangle Grid.Column="1" Fill="LightCoral" Width="100"/>
        </Expander>
    </Grid>
</Window>